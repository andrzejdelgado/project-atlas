import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Chat",
  description:
    "Ask Atlas anything about Andrzej — case studies, mentoring, projects, contact.",
};

export default function ChatPage() {
  return null;
}
